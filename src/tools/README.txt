Available Tools:

cs      - connects to the given server, hopping automatically as needed.
crowbar - attempts to gain root access to every single server.
map     - generates a map showing the shortest path to each server.
path    - shows the shortest path to a given server.
find    - shows every file on every server in the game.
get     - finds and retrieves a file with the given name.
